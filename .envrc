# Repository-specific AWS Profile Configuration
# This forces AWS CLI to use the gghouse profile for this project
# Preventing accidental use of wrong AWS account

export AWS_PROFILE=gghouse
export AWS_DEFAULT_PROFILE=gghouse
export AWS_REGION=ap-northeast-1
export AWS_DEFAULT_REGION=ap-northeast-1

# Database connection (for local development/scripts)
export DB_HOST=tokyobeta-prod-aurora-cluster-public.cluster-cr46qo6y4bbb.ap-northeast-1.rds.amazonaws.com
export DB_PORT=3306
export DB_NAME=tokyobeta
export DB_USER=admin

# S3 Configuration
export S3_DUMPS_BUCKET=jram-gghouse
export S3_DUMPS_PREFIX=dumps/

# Terraform Configuration
export TF_VAR_environment=prod

echo "✅ AWS Profile set to: $AWS_PROFILE (Account: 343881458651)"
echo "✅ Region: $AWS_REGION"
echo "✅ Database: $DB_HOST"
